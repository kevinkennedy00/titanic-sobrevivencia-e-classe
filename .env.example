# Optional local-only development value. The presentation runs from the included
# Titanic CSV and does not require a database or credentials.
TITANIC_CORS_ORIGINS=http://localhost:3011,http://127.0.0.1:3011
